datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  email     String   @id
  fullName  String
  phone     String
  age       Int
  password  String
  createdAt DateTime @default(now())
}